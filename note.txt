//check if int
//check dups


//ft_swap
	läuft auch 2x


//ft_push 
	läuft auch 2x, aber die erste zahl darf keine 0 sein

//ft_rotate_up
	läuft auch 2x

//ft_rotate_down 
	läuft auch 2x


//stack_b is not getting empty at all
	//b can not be empt at all
	//if pb gets done: the index gets lost


change printf to your own!!!
	changed to write, but make sure it is


//if the nbr is bigger the MAXINT print Error and exit
	//done: 2147483648 gets to *-1 but more then this is working


//if the stack is still sorted print new line
